import { createFoundationCapabilityManifests } from "../../src/core/foundation-capabilities.js";
export const manifest = { ...createFoundationCapabilityManifests()[0], id: "third.runtime-probe", name: "Third Runtime Probe", provenance: { source: "third-party-fixture", license: "MIT" }, inputSchema: { type: "object", additionalProperties: false, required: ["value"], properties: { value: { type: "string" } } }, outputSchema: { type: "object", additionalProperties: false, required: ["adapter", "echo"], properties: { adapter: { type: "string" }, echo: { type: "string" } } } };
export async function invoke(input, { signal } = {}) { if (input.value === "wait") await new Promise((resolve, reject) => { const timer = setTimeout(resolve, 200); signal?.addEventListener("abort", () => { clearTimeout(timer); reject(new Error("cancelled")); }, { once: true }); }); return { adapter: "third", echo: input.value }; }
